# Database
DATABASE_URL="postgresql://user:password@localhost:5432/talmudai?schema=public"

# Environment
NODE_ENV="development"

# App URL (used for metadata)
NEXT_PUBLIC_URL="http://localhost:3000"

# Skip env validation (for Docker builds)
# SKIP_ENV_VALIDATION=true
